#include <stdint.h>
int32_t main()
{
    int32_t a = 0;
    int32_t r = 0;

    if (a == 0)
    {
        int32_t a = 0;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
        if (a == 0)
        {
            r = r + a;
        }
        r = r + 32;
    }

    return r;
}